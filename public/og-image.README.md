# TODO-OG-IMAGE

Replace `/public/og-image.png` with a real 1200x630 social share image
before launch. The path `${SITE_URL}/og-image.png` is baked into every
route's `og:image` and `twitter:image` via `src/lib/seo.ts`.
